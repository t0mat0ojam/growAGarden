import SwiftUI

struct NextPageView: View {
    var body: some View {
        VStack {
            Text("Next Page")
                .font(.largeTitle)
                .padding()
            Text("Decide what goes here later.")
        }
    }
}

